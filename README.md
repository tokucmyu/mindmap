# mindmap

```mermaid
graph TD;
    A[Start] --> B[Process];
    B --> C{Decision};
    C -->|Yes| D[Result 1];
    C -->|No| E[Result 2];

%%{init:{"theme": "base"}}%%
mindmap
  root((Notionとは何か))
    定義
      ノートテイキングアプリ
      オールインワンのワークスペース
